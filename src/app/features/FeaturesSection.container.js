import { connect } from 'react-redux';
import FeaturesSection from './FeaturesSection.view';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(FeaturesSection);
