import { connect } from 'react-redux';
import CtaSection from './CtaSection.view';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(CtaSection);
