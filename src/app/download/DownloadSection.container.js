import { connect } from 'react-redux';
import DownloadSection from './DownloadSection.view';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(DownloadSection);
