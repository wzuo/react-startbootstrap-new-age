import { connect } from 'react-redux';
import ContactSection from './ContactSection.view';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ContactSection);
